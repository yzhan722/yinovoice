# Yino Voice Stage1 能力范围汇报

- 日期：2026-08-18
- 用途：内部阶段汇报（能力边界、已交付、明确不做）
- 事实来源：`TASKS.md`、`PROJECT_STATUS.md`、`docs/platform/` 现行规格，以及 Control Plane / Runtime 代码
- 仓库：`E:\Repos\yinovoice`（主仓）；本地对照 worktree 不作为本汇报部署真源
- 部署边界：**Stage1 隔离栈**，不上生产

---

## 0. 一句话结论

Stage1 已经从「本机麦克风语音闭环 Demo」推进到「可隔离部署的网页语音前台 + 租户配置 + 通话留痕 + 预约/回拨意向闭环」。  
它能证明 **Yino 自管配置与业务数据、Qwen Realtime 实时对话、通话结束后自动登记意向**；**还不能**对外宣称已具备电话接入、真实挂号、真实外呼或生产级多租户。

汇报口径建议：

> 当前是 **内部/受邀验收的 Stage1 平台切片**，不是面向真实患者的生产客服。

---

## 1. 阶段定位

| 维度 | Stage1 现状 |
|------|-------------|
| 产品目标 | 可复用 AI 语音客服平台；第一行业样例为口腔前台（太平洋口腔演示 Prompt） |
| 接入形态 | **浏览器实时语音**（麦克风 ↔ LiveKit ↔ Qwen Audio Realtime） |
| 数据主权 | Yino 是配置与业务数据的唯一事实来源；Vapi 仅为后续适配/备用渠道，不在主链路 |
| 租户模型 | Demo 分区：`X-Tenant-ID` 头隔离；登录为前端 shell mock（`demo` / `admin`） |
| 持久化 | PostgreSQL；Stage1 独立库 `yino_platform_stage1` |
| 验收环境 | `https://<HOST>/stage1/`，进程与库与生产隔离 |
| 生产 | `https://8.215.80.82/` 保持回滚基线，**本阶段不覆盖** |

Stage1 要回答的问题是：**平台核心能否独立跑通「听—说—记—转意向」**，而不是「能否替代诊所现有挂号系统和电话总机」。

---

## 2. 系统构成

```text
浏览器（Vue 3 控制台）
    │  实时音视频 + 配置/记录 API
    ▼
LiveKit（房间与媒体）
    │  named-agent 调度 + 短期 token
    ▼
Runtime voice-agent
    │  Qwen Audio Realtime（默认）
    ▼
Platform API（Control Plane）
    │  实例 / 通话 / 预约 / 回拨
    ▼
PostgreSQL（租户隔离表）
```

| 组件 | 路径 | 职责 |
|------|------|------|
| 控制台前端 | `apps/control-plane/web` | 租户工作台、实例、实时语音、通话、预约、回拨、知识库配置 |
| Platform API | `apps/control-plane/api` | 配置、LiveKit token、通话记录、录音元数据、预约/回拨 CRUD、意向抽取 |
| Voice Agent | `apps/runtime/voice-agent` | Qwen Realtime 双向音频；打断（barge-in）；按实例配置合成 Prompt |
| 数据库 | Docker Postgres，Stage1 独立库 | 实例、通话、消息、预约、回拨；重启不丢 |

默认实时链路：

```text
浏览器麦克风 → LiveKit → Qwen Audio Realtime → 立即回放 → 浏览器扬声器
```

- 模型默认：`qwen-audio-3.0-realtime-plus`
- 音色默认：`longanqian`（实例可改）
- 话轮：`smart_turn`；用户 partial/final 转写；客服增量文本 + 音频
- 首包音频在 `response.audio.delta` 即播，不等 `response.done`
- 用户插话会打断当前回复，并隔离迟到的旧音频/文本

可选回退：`Fun-ASR + LLM + TTS` pipeline（非默认、不作为 Stage1 主验收）。

---

## 3. 已交付能力（按模块）

### 3.1 实时语音对话 — 已可用

| 能力 | 状态 | 说明 |
|------|------|------|
| 浏览器进房通话 | 已交付 | 点击开始后才申请麦克风；短期 LiveKit token |
| 多轮普通话对话 | 已交付 | 上下文保持；可做 1–2 分钟口腔前台演示 |
| 增量转写与客服字幕 | 已交付 | partial 收敛为 final；final 写入通话记录 |
| 低延迟播报 | 已交付 | 首包即播，连续播放 |
| 自然打断 | 已交付 | barge-in；被打断回复不回放、不积压 |
| 按实例加载配置 | 已交付 | 欢迎语、双层 Prompt、音色、回复风格随实例 UUID 下发 |
| 安全红线 | 已交付 | 平台硬编码规则优先于 Tenant Prompt；禁止确诊/开药/编造价格/假装已约成功 |

**不能说成：** 已接通真实电话、SIP、手机外呼。

### 3.2 语音 Agent 实例 — 已可用（A1–A3）

租户可管理多套可运行配置（对外仍兼容 `/api/v1/customer-services`）。

| 能力 | 状态 |
|------|------|
| 租户内列表 / 分页 / 真实 UUID 选择 | 已交付 |
| 网页新建实例（预填合成演示文案，可改） | 已交付 |
| 编辑展示名、机构、欢迎语、音色、双 Prompt | 已交付 |
| 乐观锁版本冲突（409，提示刷新） | 已交付 |
| 软删除 / 恢复；硬删仅限已软删且无关联通话 | 已交付 |
| 列表默认隐藏已删，可「显示已删除」 | 已交付 |

双层 Prompt：

1. 平台硬编码红线（不可被租户覆盖）
2. `platform_prompt`（运营/管理员维护的对话逻辑）
3. 边界声明
4. `tenant_prompt`（租户业务知识与话术）

演示种子：太平洋口腔（常州）前台——地址/分院/营业时间、种植与正畸科普、症状分诊提问、转人工指引。

### 3.3 通话记录与浏览器录音 — 已可用（部分）

| 能力 | 状态 | 边界 |
|------|------|------|
| 通话结束后落库（元数据 + final 转写） | 已交付 | 网页 Demo 记录，不是电话 CDR |
| 列表 / 详情 / 改 / 软删 / 恢复 | 已交付 | 租户隔离；软删后对用户 404 |
| 浏览器混录麦克风 + 客服音轨并上传 | 已交付 | WebM 本地目录；可关闭 |
| 详情页播放已就绪录音 | 已交付 | 无 Egress / SIP 录音 / 对象存储 |
| 全文搜索、完整审计后台 | 未做 | TASKS B 剩余项 |

重启 API 后，库内记录仍在；磁盘上的孤儿录音文件不会自动清理。

### 3.4 预约结果 — 已可用（意向层，非挂号权威）

| 能力 | 状态 |
|------|------|
| 网页新建 / 列表 / 改状态 / 取消（软语义） | 已交付 |
| 租户隔离，落 Postgres | 已交付 |
| 通话结束自动抽取预约意向（`source=voice_tool`） | 已交付 |
| 列表标注「语音自动」，可跳转关联通话 | 已交付 |
| 缺电话/时段仍建预约，notes 标明待确认 | 已交付（启发式，非 LLM Tool） |

**不能说成：** 已对接诊所排班、医生资源、档期冲突或外部预约系统。Scheduling Authority 仍在诊所侧，本阶段只登记意向。

### 3.5 回拨任务 — 已可用（任务队列，非外呼）

| 能力 | 状态 |
|------|------|
| 网页新建 / 完成 / 重开 | 已交付 |
| 通话结束抽取「仅回电」意向 | 已交付 |
| 页头明确：本阶段不发起真实外呼 | 已交付 |

**不能说成：** 已能自动拨出电话或转接坐席。

### 3.6 知识库页 — 半交付

| 能力 | 状态 |
|------|------|
| 绑定当前实例，改音色 | 已交付 |
| 租户编辑 `tenant_prompt`；底层 Prompt 只读 | 已交付 |
| 资料上传列表 / RAG 检索进实时通话 | **演示框架**，未接入 |

知识目前主要通过 **Prompt 文本** 进入对话，不是独立知识库检索。

### 3.7 工作台 / 运营端 — 壳层可见

| 页面 | 状态 |
|------|------|
| 租户工作台 KPI、趋势、待办 | 仍走前端 mock/演示汇总，**未全部绑定真实预约/回拨 API** |
| 运营端通话记录 | 可查看固定 Demo Tenant 的同一批记录 |
| 运营端模板市场 | 菜单占位，不是已发布模板产品 |
| 学习计划 / 成就等 Learnova 遗留页 | 隐藏菜单，与语音主线无关 |

---

## 4. 能力对照：PRD 目标 vs Stage1

对照内部 PRD「Platform Core / 通用接待员」目标，用三档标注，避免把规划说成交付。

| PRD 目标 | Stage1 | 说明 |
|----------|--------|------|
| 接听入站电话 / SIP | 未做 | 仅浏览器 |
| 实时会话与通话状态 | 已做 | LiveKit 房间 + 页面状态 |
| 可替换 Transcriber / Model / Voice | 部分 | 默认一体 Realtime；音色可换；拆分 pipeline 仅回退 |
| Prompt + 知识库生成回答 | 部分 | 双 Prompt 已做；独立知识库未进链路 |
| Schema 校验的工具调用 | 未做 | Qwen Realtime 无原生 Function Tool；用通话结束规则抽取代替 |
| 自然打断 | 已做 | |
| 转人工 / 回拨任务 / 人工通知 | 部分 | 回拨任务队列已做；无真转接、无自动通知渠道 |
| 通话记录、工具结果、阶段耗时 | 部分 | 记录与转写已做；无完整耗时/工具审计 |
| Tenant 隔离 | 部分 | 数据按 `tenant_id` 隔离；非生产认证/RBAC |
| 大陆区域准入的 Provider | 部分 | 当前固定国内 Qwen Realtime；无完整准入工作流 |
| 查询空档 / 真实预约改取消 | 未做 | 只登记意向 |
| 写操作前复述确认 | Prompt 约束 | 模型遵守话术，无系统级预约确认状态机 |

---

## 5. 明确不在 Stage1 范围

汇报时建议主动列出，防止听众按「完整电话客服」理解：

- 真实 SIP / 运营商号码 / 主动外呼 / 诊后回访营销电话
- 面向真实患者的公开生产服务
- 生产级身份认证、RBAC、套餐计费、发票
- Google Calendar / 诊所管理系统正式连接器
- 医生排班、项目时长、档期冲突引擎
- 配置发布：版本 Diff / 回读 / 回滚产品化（原则已定，产品未做完）
- 录音转自有 S3；跨区域复制；完整 SLA
- 模板市场、第三方模板交易
- 完整 Vapi Adapter（Vapi 不在当前主链路）
- AI 医疗诊断或个性化治疗决策
- 开场强制声明 AI 身份或法定录音告知（Demo 未按生产合规验收）

---

## 6. 建议演示剧本（5–8 分钟）

适合汇报现场，一条主线即可：

1. **登录租户**（`demo`）→「我的实例」：展示已有实例或新建一条非空演示实例。
2. **知识库/配置**：改一句业务 Prompt 或切换音色并保存。
3. **实时语音**：进房，完成至少 3 轮（问候 → 问地址/项目 → 「想约周五洁牙」）。
4. **打断**：客服说话时插话，确认旧音频不回放。
5. **结束通话** →「通话记录」：有转写，必要时可播放浏览器录音。
6. **预约结果**：出现「语音自动」意向（缺电话会标待确认）。
7. **回拨任务**（可选）：若只说「请回电」则进任务队列，强调**不会真拨出去**。
8. **边界说明**：问价格/确诊时，模型应拒绝编造并引导到店/人工。

运营视角：用 `admin` 打开「通话记录」，证明同一 Demo Tenant 记录可见。  
不要在汇报材料中粘贴完整对话、患者信息和任何 API Key。

---

## 7. 环境与隔离（对干系人）

| 项 | 生产 | Stage1 |
|----|------|--------|
| 入口 | `https://8.215.80.82/` | `https://8.215.80.82/stage1/` |
| 目录 | `/opt/yino-vapi` | `/opt/yino-vapi-stage1` |
| 数据库 | `yino_platform`（勿改） | `yino_platform_stage1` |
| 实例/预约等新 API | 回滚后列表/新建为 404 | 可用 |
| 部署授权 | 未获许可不得改 | 由独立脚本部署，不覆盖生产前端 |

本地开发真源在 monorepo `apps/`；密钥只存在未跟踪的 `.env.local`。

---

## 8. 已完成与未完成（对照 TASKS）

**已完成（代码与 Stage1 规格，截止 2026-08-18）**

- A1 实例列表与真实 UUID 选择
- A2 网页新建 + 合成预填
- A3 实例软删除 / 恢复 / 条件硬删
- 通话记录软删除 CRUD
- 预约/回拨 Phase 1 网页真实化
- 预约/回拨 Phase 2 通话结束意向抽取
- Stage1 隔离部署与独立库迁移

**仍待人工/浏览器确认**

- Stage1 网页冒烟：列表、新建、配置、通话、预约/回拨全路径点一遍
- GitHub 远程审查与推送策略（本地可有 commit，不以聊天假定已上远程）

**短期未做**

- 其余脚本路径收敛、`deploy/src` 与 `apps/` 去重
- 录音 S3、配置发布 Diff/回滚、Vapi Adapter 占位补齐

**中期未做**

- 真实预约系统、SIP 电话、生产多租户 RBAC

---

## 9. 风险与诚实边界

| 风险 | 汇报时应怎么说 |
|------|----------------|
| 听众把「预约」理解成已挂号 | 这是 **意向登记**；缺字段会占位并标待确认 |
| 听众把「回拨」理解成系统会打电话 | 这是 **人工待办队列**，Stage1 不拨号 |
| 工作台数字很好看 | 工作台 KPI 仍有演示层；请以预约/回拨/通话三个列表页为准 |
| Qwen 无原生 Tool | 用通话结束规则抽取，可测、可幂等，但不是通话中实时写库 |
| Demo 登录与 `X-Tenant-ID` | 证明数据分区契约，**不证明**生产安全 |
| 知识库上传按钮 | 资料列表未进检索；改 Prompt 才会影响下一通对话 |
| 生产与 Stage1 并列 | 验收只走 `/stage1/`；生产保持不动 |

---

## 10. 建议的下一阶段（仅方向，非本期承诺）

按依赖排序，便于汇报问答：

1. **把工作台 KPI 接到真实预约/回拨/通话统计**，去掉演示数字。
2. **知识库检索进实时链路**（或明确「本阶段只靠 Prompt」）。
3. **真实预约权威**：指定医生、时长、档期冲突——对接 Scheduling Authority，而不是继续加平台内日历。
4. **SIP / 电话通道**：浏览器闭环稳定后再接号码。
5. **生产身份与 RBAC**：替换 Demo 头与 mock 登录。
6. **录音对象存储与配置发布回滚**。

---

## 11. 附录：租户控制台模块一览

| 菜单 | 路由 | Stage1 可用性 |
|------|------|----------------|
| 工作台 | `/user/dashboard` | 壳 + 演示汇总 |
| 我的实例 | `/user/assistant-settings` | 真实 API |
| 实时语音 | `/user/realtime-voice` | 真实链路 |
| 回拨任务 | `/user/callback-tasks` | 真实 CRUD，不外呼 |
| 预约结果 | `/user/appointments` | 真实 CRUD + 语音意向 |
| 通话记录 | `/user/call-history` | 真实 CRUD + 可选录音 |
| 知识库 | `/user/knowledge-base` | Prompt/音色真实；资料列表演示 |
| 运营通话记录 | `/admin/call-history` | 查看同一 Demo Tenant |

---

*本文只描述 Stage1 已落地能力与边界，不构成生产上线或对外商务承诺。*
