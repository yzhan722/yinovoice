# 迁移完成报告

生成时间：2026-08-12  
目标本地仓库：`E:\Repos\yinovoice`  
远程：`https://github.com/yzhan722/yinovoice.git`（整理前为空）  
操作：复制整理；**未** commit / push；**未**修改源项目。

## 执行范围（已确认默认）

| 项 | 结果 |
|----|------|
| 核心 Stage1 平台 | 已复制 |
| 根文档 / 平台文档 | 已复制 |
| LAN platform-core / deploy / integrations | 已复制 |
| archive 原型 | 本轮未纳入 |
| livekit-server.exe | 本轮未纳入 |
| gh CLI | 不依赖 |

## 实际落盘结构（主要）

```text
yinovoice/
├── README.md, AGENTS.md, PROJECT_STATUS.md, DECISIONS.md, TASKS.md
├── SECURITY.md, CONTRIBUTING.md, CONTEXT.md
├── .gitignore, .env.example
├── .cursor/rules/project-rules.mdc
├── docker-compose.yml, docker-compose.server.yml
├── apps/control-plane/api|web
├── apps/runtime/voice-agent
├── packages/platform-core
├── integrations/
├── deploy/          # 已移除 frontend-dist 与疑似正式 *.env
├── docs/… + docs/migration/
└── scripts/
```

## 源 → 目标摘要

| 源 | 目标 |
|----|------|
| `...\YinoVoicePlatform\platform-api` | `apps/control-plane/api` |
| `...\YinoVoicePlatform\front` | `apps/control-plane/web` |
| `...\YinoVoicePlatform\voice-agent` | `apps/runtime/voice-agent` |
| `...\YinoVoicePlatform\scripts` | `scripts` |
| `E:\YinoVapi\docs` | `docs/source-root` |
| `...\YinoVoicePlatform\docs` | `docs/platform` |
| NAS `services/platform-core` | `packages/platform-core` |
| NAS `deploy-package` | `deploy` |
| NAS `integrations` | `integrations` |

## 排除与隔离

- 排除：`.git`（源）、`node_modules`、`.venv`、`dist`、录音 `*.webm`、各类 `.env`（保留 `.env.example`）  
- 自目标移出（本机 quarantine，**不在仓库内**）：  
  - `deploy/config/platform-api.env`  
  - `deploy/config/voice-agent.env`  
  位置：`E:\Repos\_yinovoice-migration-reports\quarantine-excluded-from-repo\`  
- 删除构建产物：`deploy/src/frontend-dist`  

## 规模（验收时统计，约）

见《GitHub上传前检查.md》中的文件数与容量。

## 独立 Git 仓库

- 源 `E:\YinoVapi` 及其 worktree：保留原样，历史未并入  
- `ai-voice-management` 独立仓库：本轮未复制  
- 目标仅含一个 `.git`（yinovoice 克隆本身）  

## 后续

交由用户在 GitHub Desktop 检查后人工提交推送。
