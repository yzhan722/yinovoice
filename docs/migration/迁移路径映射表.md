# 迁移路径映射表

生成时间：2026-08-12  
原则：以本地 `feat/yino-voice-stage1` 为主源；LAN 补 deploy / platform-core / integrations；复制时排除 `.git`、密钥、录音、缓存。  
状态：**方案待确认，未复制**。

## A. 核心产品（优先）

| 源绝对路径 | 目标（相对 `E:\Repos\yinovoice`） | 方式 | 备注 |
|------------|-------------------------------------|------|------|
| `E:\YinoVapi\.worktrees\yino-voice-stage1\YinoVoicePlatform\platform-api\` | `apps/control-plane/api/` | 复制（排除 `.venv`、`.env*`、`data/recordings/`） | Control Plane |
| `E:\YinoVapi\.worktrees\yino-voice-stage1\YinoVoicePlatform\front\` | `apps/control-plane/web/` | 复制（排除 `node_modules/`、`dist/`、`.env*`） | Web |
| `E:\YinoVapi\.worktrees\yino-voice-stage1\YinoVoicePlatform\voice-agent\` | `apps/runtime/voice-agent/` | 复制（排除 `.venv`、`.env*`） | Runtime |
| `E:\YinoVapi\.worktrees\yino-voice-stage1\YinoVoicePlatform\scripts\` | `scripts/` | 复制 | |
| `E:\YinoVapi\.worktrees\yino-voice-stage1\YinoVoicePlatform\docker-compose*.yml` | 根或 `deploy/` | 复制 | 检查硬编码密码后决定是否改示例 |
| `E:\YinoVapi\.worktrees\yino-voice-stage1\YinoVoicePlatform\README.md` | 合并进根 `README.md` | 合并方案 | 不覆盖式盲目写 |

## B. 文档与术语

| 源绝对路径 | 目标 | 方式 | 备注 |
|------------|------|------|------|
| `E:\YinoVapi\CONTEXT.md` | `CONTEXT.md` | 复制 | |
| `E:\YinoVapi\docs\` | `docs/`（按 architecture/migration/api 等归类） | 复制+归类 | 与平台 docs 去重 |
| `E:\YinoVapi\.worktrees\yino-voice-stage1\YinoVoicePlatform\docs\` | `docs/platform/` 或合并 | 复制+去重 | |
| `E:\YinoVapi\.worktrees\yino-voice-stage1\docs\superpowers\` | `docs/superpowers/` | 可选 | |

## C. LAN 独有增量（只读源）

| 源绝对路径 | 目标 | 方式 | 备注 |
|------------|------|------|------|
| `\\192.168.110.113\data\company\yinoai\YinoVapi\services\platform-core\` | `packages/platform-core/` | 复制 | 知识库适配 |
| `\\192.168.110.113\data\company\yinoai\YinoVapi\deploy-package\` | `deploy/` | 复制（排除 secrets） | 先抽检 |
| `\\192.168.110.113\data\company\yinoai\integrations\` | `integrations/` | 复制（排除 `.env`、本地 mapping） | |

## D. Stage2 / 移动端（选择性，需单独确认）

| 源绝对路径 | 目标 | 方式 | 备注 |
|------------|------|------|------|
| `E:\YinoVapi\YinoVapi\apps\admin\` 中 Capacitor/android 增量 | 合并入 `apps/control-plane/web/` | 选择性合并 | 避免整树复制 |
| `E:\YinoVapi\YinoVapi\bin\livekit-server.exe` | `bin/` 或文档说明外部安装 | **待确认** | 约 38.5 MB |

## E. 原型归档（可选，需确认）

| 源绝对路径 | 目标 | 方式 | 备注 |
|------------|------|------|------|
| `E:\YinoVapi\STT-LLM-Hello World\` | `archive/prototypes/stt-llm-hello-world/` | 复制（排除 `.venv`、`.env`） | |
| `E:\YinoVapi\.worktrees\fun-asr-workbench\` | `archive/prototypes/fun-asr-workbench/` | 复制（排除 `.venv`） | 不带 `.git` |
| `E:\YinoVapi\.worktrees\minimal-voice-chat\Voice-STT-LLM-TTS-HelloWorld\` | `archive/prototypes/minimal-stt-llm-tts/` | 复制（排除 `.venv`、`.env`） | |
| `E:\YinoVapi\BaseVapiYinoai\ai-voice-management\` | `archive/upstream/ai-voice-management/` | 复制且**排除 `.git`** | 原历史不带入；需单独确认 |
| `...\YinoVoicePlatform\web-console\` | `archive/legacy/web-console/` | 可选 | 已废弃 |

## F. 占位（仅在确认需要时创建，不机械建空目录）

| 目标 | 说明 |
|------|------|
| `packages/vapi-adapter/` | 当前无独立包；若确认需要则放 README 说明定位 |
| `packages/shared-types/` / `shared-config/` | 仅在有可迁入代码时创建 |
| `prompts/templates/` | 仅在有脱敏提示词时创建 |

## G. 明确不映射（排除）

见《敏感文件风险报告》与《待确认问题》；包括但不限于：所有 `.env*`（除 example）、录音 WebM、node_modules、.venv、LAN 重复 front/Demo/stage2 副本、zip 包、`__MACOSX`。
