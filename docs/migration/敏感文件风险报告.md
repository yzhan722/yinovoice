# 敏感文件风险报告

生成时间：2026-08-12  
原则：只报告路径与风险类型；**不显示、不复制、不记录密钥内容**。

## 1. 疑似密钥 / 环境配置（禁止进入 yinovoice）

| 路径 | 风险类型 |
|------|----------|
| `E:\YinoVapi\.worktrees\yino-voice-stage1\YinoVoicePlatform\platform-api\.env.local` | 本地 API / LiveKit 开发凭据 |
| `E:\YinoVapi\.worktrees\yino-voice-stage1\YinoVoicePlatform\voice-agent\.env.local` | DashScope / 模型服务密钥类 |
| `E:\YinoVapi\.worktrees\yino-voice-stage1\YinoVoicePlatform\front\.env.local` | 前端本地配置 |
| `E:\YinoVapi\.worktrees\yino-voice-stage1\YinoVoicePlatform\front\.env.release` | 构建环境配置 |
| `E:\YinoVapi\YinoVapi\apps\admin\.env.development` 等 | 多环境前端配置 |
| `E:\YinoVapi\BaseVapiYinoai\ai-voice-management\.env.development` 等 | 前端环境配置；曾出现 **git staged** → 泄露风险高 |
| `E:\YinoVapi\.worktrees\minimal-voice-chat\Voice-STT-LLM-TTS-HelloWorld\.env` | 原型 API Key 类 |
| `\\192.168.110.113\data\company\yinoai\front\.env.*` | 前端环境配置 |
| `\\192.168.110.113\data\company\yinoai\YinoVoiceDemo\**\.env.local` | Demo 本地配置 |
| `\\192.168.110.113\data\company\yinoai\integrations\ragflow\.env` | RAGFlow secrets |
| `\\192.168.110.113\data\company\yinoai\integrations\ragflow\mapping.yaml` | 本地租户映射（敏感） |
| `YinoVoicePlatform\docker-compose.yml` 内数据库口令占位 | 开发口令（迁移时改为 `.env.example` 说明） |

未在扫描范围内发现独立 `*.pem` / `*.key` 文件报告项（仍按规则全局排除）。

## 2. 通话录音 / 导出（禁止进入仓库）

| 文件 | 约大小 | 路径 |
|------|--------|------|
| `322931c4-6127-496e-a9f1-fa6302c7a140.webm` | 0.92 MB | `...\platform-api\data\recordings\00000000-0000-0000-0000-000000000001\` |
| `3391f0df-ac31-4948-a50c-d8acab62cb80.webm` | 0.42 MB | 同上 |
| `73e02122-fab1-4426-9b2c-8bcb24379c0a.webm` | 0.78 MB | 同上 |

## 3. 隐私 / 医疗主题风险

| 路径/类型 | 风险 |
|-----------|------|
| 上述 WebM | Demo 通话录音，可能含语音 |
| `docs/testing/dental-call-recording-protocol.md` | 牙科录音测试协议（流程文档，可评估后脱敏纳入） |
| `integrations/ragflow/demo-docs/*.md` | 合成牙科 FAQ（演示数据，非真实患者） |
| LAN `docs/debug-latest-call-*.md` | 调试日志，可能含通话元数据/转写片段 → **默认排除** |

## 4. 大文件

| 大小 | 路径 | 处理 |
|------|------|------|
| ~65.5 MB | `voice-agent\.venv\...\livekit\...` | **排除**（venv） |
| ~38.5 MB | `E:\YinoVapi\YinoVapi\bin\livekit-server.exe` | 待确认是否纳入；不启用 LFS 除非你确认 |
| ~11.5 MB | LAN `ai-voice-management.zip` | 排除（已有解压目录） |
| >100 MB | 未发现 | |

## 5. 缓存 / 构建产物（排除）

包括但不限于：`node_modules/`、`dist/`、`dev-dist/`、`.venv/`、`__pycache__/`、`.pytest_cache/`、`*.log`。

## 6. 独立 Git / 错误远程

| 路径 | 风险 |
|------|------|
| `E:\YinoVapi\BaseVapiYinoai\ai-voice-management\.git` | 不得嵌套进 yinovoice；远程指向外部 OA 仓库 |
| 复制该项目时必须排除内部 `.git` | 原历史不带入 |

## 7. 处理原则（第三阶段将遵守）

- 只复制到 `E:\Repos\yinovoice`，不改源  
- 疑似密钥只排除并记入本报告  
- 录音、客户隐私、正式配置一律不入库  
- `.env.example` 仅变量名与安全占位值  

## 8. 第三阶段隔离结果（2026-08-12）

以下文件曾被复制进目标，因疑似非占位密钥/正式配置，已**移出仓库**到本机 quarantine（不进入 Git）：

| 原目标路径 | 隔离目录 |
|------------|----------|
| `deploy/config/platform-api.env` | `E:\Repos\_yinovoice-migration-reports\quarantine-excluded-from-repo\` |
| `deploy/config/voice-agent.env` | 同上 |

另删除构建产物：`deploy/src/frontend-dist/`。  
