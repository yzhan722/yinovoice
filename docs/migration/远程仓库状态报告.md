# 远程仓库状态报告

生成时间：2026-08-12  
报告目录（克隆目录之外）：`E:\Repos\_yinovoice-migration-reports`  
阶段：第一阶段完成核验（`gh` 本机仍不可用，部分项依赖 GitHub Desktop / git 间接确认）

## 1. 账号与仓库关系

| 项 | 值 | 判断 |
|---|---|---|
| 仓库所有者 | `yzhan722` | |
| 仓库名称 | `yinovoice` | |
| 仓库 URL | https://github.com/yzhan722/yinovoice | |
| 期望 origin | https://github.com/yzhan722/yinovoice.git | |
| GitHub Desktop 登录账号（用户确认） | `Zezeyu01` | ≠ 所有者，属预期 |
| 本机 Git 提交用户名 | `Zzzzze` | |
| 本机 Git 提交邮箱 | `6kvnxw9vpv@privaterelay.appleid.com` | |
| 当前账号是否可在 Desktop 看到仓库 | 是（已成功克隆） | 至少有读权限 |
| 当前账号是否可克隆 | 是 | 已克隆到本地 |
| 当前账号是否可推送 | **未最终用 API 确认** | 空仓库首次推送需写权限；建议用 Desktop 试推或装好 `gh` 后查 `viewerPermission` |
| `gh` CLI | **本机当前会话找不到可用 `gh.exe`** | `E:\gh CLI` 目录存在但为空；`winget list` 无 `GitHub.cli` |

## 2. 本地克隆核验

| 项 | 值 |
|---|---|
| 本地克隆绝对路径 | `E:\Repos\yinovoice` |
| 是否 Git 仓库 | 是 |
| `origin` fetch/push | `https://github.com/yzhan722/yinovoice.git` |
| 当前本地分支 | `main`（尚无提交） |
| 工作区文件 | 仅 `.git`（无业务文件） |
| 未提交修改 | 无业务文件可改；状态：`No commits yet on main...origin/main [gone]` |
| 相对远程 | 远程亦无有效提交历史（空仓库） |
| HEAD | 尚无 commit |

## 3. 远程仓库内容

| 项 | 值 | 依据 |
|---|---|---|
| 远程是否存在 | 是 | 可克隆 + `git ls-remote` 成功 |
| 可见性（公开/私有） | **未用 API 确认** | 需 `gh` 或网页查看 |
| 默认分支 | 预期 `main` | 本地跟踪 `origin/main` |
| 是否已有提交 | **否（空仓库）** | `git ls-remote` 无引用；本地无 commit |
| 分支 | 无实质分支历史 | |
| 标签 | 未见 | |
| 已有文件 | 无 | |
| 分支保护 / PR 要求 / rulesets | **未确认** | 需 `gh api .../protection` 或网页 Settings |

## 4. 权限结论（当前）

1. `Zezeyu01` **可以访问并克隆** `yzhan722/yinovoice` → 读权限成立。  
2. 仓库目前为空 → 后续整理后由你在 GitHub Desktop **人工首次提交并推送**。  
3. 若推送被拒：请所有者为 `Zezeyu01` 增加 write，或允许通过 PR 合并（空仓库通常需要直接 push 才能建立首提交）。  
4. 未修改任何远程权限/可见性；未 push；未 commit。

## 5. 代理修复记录（克隆前置条件）

| 项 | 值 |
|---|---|
| 原问题 | Git 代理 `127.0.0.1:7890` 无监听导致克隆失败 |
| 修复 | 全局代理改为 `http://127.0.0.1:7897`（`verge-mihomo`） |
| 现状 | 克隆已完成 |

## 6. `gh` 待补齐

请确认 `gh` 是否真正安装成功：

```cmd
where gh
gh --version
gh api user --jq .login
gh repo view yzhan722/yinovoice --json visibility,viewerPermission,isEmpty,defaultBranchRef
```

若 `where gh` 找不到，说明只建了文件夹或安装未完成。建议将便携版解压到例如 `E:\Repos\tools\gh\`，并把 `bin` 加入 PATH 后重开终端。

补齐后可更新本报告中的：可见性、`viewerPermission`、分支保护。

## 7. 与后续整理的关系

- 远程为空 → **无“覆盖远程已有内容”冲突**；仍以远程空仓库为基底，向克隆目录复制整理成果。  
- 禁止自动 commit/push；完成后交 GitHub Desktop 人工检查。
