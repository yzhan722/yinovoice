# 迁移前盘点报告

生成时间：2026-08-12  
扫描范围：`E:\YinoVapi`（含 `.worktrees`）、`\\192.168.110.113\data\company\yinoai`（只读）  
目标克隆：`E:\Repos\yinovoice`（空仓库）  
约束：只读；未移动/删除源文件；未读取 `.env` 内容；未显示密钥明文。

## 执行摘要

| 维度 | 结论 |
|------|------|
| 主产品形态 | 浏览器实时语音 Demo：`front` + `platform-api`（Control Plane）+ `voice-agent`（Runtime）+ LiveKit + Qwen Realtime |
| 最活跃源码 | `E:\YinoVapi\.worktrees\yino-voice-stage1\YinoVoicePlatform`（分支 `feat/yino-voice-stage1`，约 39 文件未提交变更） |
| 最完整 LAN 副本 | `\\192.168.110.113\data\company\yinoai\YinoVapi`（含 `services/platform-core`、`deploy-package`、`_remote_deploy`） |
| 主 Git 根 | `E:\YinoVapi`（含多个 worktree）；**无 remote** |
| 独立嵌套 Git | `BaseVapiYinoai\ai-voice-management`（远程指向外部 OA 前端，归属不匹配） |
| 重复 | `YinoVoicePlatform` / 管理前端 / `docs` / `CONTEXT.md` 多处拷贝 |
| 敏感风险 | 多处 `.env*`；`ai-voice-management` 中 `.env*` 曾被 staged；Demo WebM 录音 3 个 |
| 大文件 | 无 >100MB；>50MB 仅 `.venv` 内 LiveKit FFI（排除） |
| 建议迁入规模（粗估） | 去重后约 **2500–3500 文件 / ~20–25 MB**（排除 node_modules、.venv、.git、录音、密钥） |
| 与远程冲突 | **低**（远程空仓库） |

## Git / Worktree 拓扑

```
E:\YinoVapi\.git  (main, 无 remote)
├── E:\YinoVapi                              [main]
├── .worktrees\fun-asr-workbench           [feature/fun-asr-workbench]
├── .worktrees\minimal-voice-chat          [codex/minimal-voice-chat]
└── .worktrees\yino-voice-stage1           [feat/yino-voice-stage1]  ← Stage1 主开发线
```

| 路径 | 分支 | Remote | 未提交 |
|------|------|--------|--------|
| `E:\YinoVapi` | `main` | 无 | 有 |
| `.worktrees\yino-voice-stage1` | `feat/yino-voice-stage1` | 无 | 有（约 39 文件） |
| `.worktrees\fun-asr-workbench` | `feature/fun-asr-workbench` | 无 | 干净 |
| `.worktrees\minimal-voice-chat` | `codex/minimal-voice-chat` | 无 | 干净 |
| `BaseVapiYinoai\ai-voice-management` | `dev-AI语音客服` | 外部 OA 仓库 | 有 staged（含疑似 env） |
| `E:\Repos\yinovoice` | `main` | GitHub origin | 空，无提交 |

## 项目清单（摘要）

| # | 名称 | 绝对路径 | 类型 | 技术栈摘要 | Voice 相关 | 有 `.git` | 建议 |
|---|------|----------|------|------------|------------|-----------|------|
| 1 | YinoVapi 元仓库 | `E:\YinoVapi` | 文档/worktree 容器 | Git + Markdown | 间接 | 是 | 纳入根文档 |
| 2 | YinoVoicePlatform | `E:\YinoVapi\.worktrees\yino-voice-stage1\YinoVoicePlatform` | 全栈 Demo | Vue3/Vite、FastAPI、livekit-agents | **直接** | 否（属 worktree） | **核心纳入** |
| 3 | YinoVapi 嵌套副本 | `E:\YinoVapi\YinoVapi` | Stage2 快照 | 同上 + Capacitor | **直接** | 否 | 部分合并 |
| 4 | ai-voice-management | `E:\YinoVapi\BaseVapiYinoai\ai-voice-management` | 管理前端原型 | Vue3/TDesign | **直接** | **是（独立）** | archive，排除其 `.git` |
| 5 | STT-LLM-Hello World | `E:\YinoVapi\STT-LLM-Hello World` | 探针 | FastAPI/百炼 | 间接 | 否 | archive |
| 6 | fun-asr-workbench | `E:\YinoVapi\.worktrees\fun-asr-workbench` | STT 工作台 | FastAPI/Fun-ASR | 间接 | 属主仓 worktree | archive |
| 7 | minimal-voice-chat | `E:\YinoVapi\.worktrees\minimal-voice-chat` | 原型+文档 | FastAPI | 间接 | 属主仓 worktree | archive 部分 |
| 8 | 根 docs | `E:\YinoVapi\docs` | 文档 | Markdown | **直接** | 主仓内 | 可选纳入 `docs/` |
| 9 | LAN YinoVapi | `\\192.168.110.113\data\company\yinoai\YinoVapi` | 全栈+部署 | 含 platform-core/deploy | **直接** | 有（ownership 限制） | 合并 LAN 独有 |
| 10 | LAN integrations | `\\192.168.110.113\data\company\yinoai\integrations` | 集成配置 | RAGFlow YAML | **直接** | 否 | 纳入（排除 secrets） |
| 11 | LAN front / Demo / stage2 副本 | 多路径 | 重复副本 | — | 直接 | 部分有 | **排除重复** |

## 架构原则（保留，供后续标准化）

1. Yino 是业务数据与配置的唯一事实来源  
2. Vapi 是执行适配器 / 兼容与备用渠道  
3. 不把 Vapi Workflows 作为核心依赖  
4. n8n 只做异步自动化，不进实时通话主链路  
5. 后续运行时方向：LiveKit Agents + SIP  
6. 配置发布需版本、校验、回读、Diff、测试、回滚  
7. 通话录音转存自有 S3 兼容存储  
8. 客户数据迁移可审计、可回滚  
9. Customer/Agent/Assistant/Conversation/Usage 由 Yino 自管  
10. 外部语音平台不得成为业务数据唯一存储  

## 建议迁入规模

| 范围 | 文件数粗估 | 容量粗估 |
|------|------------|----------|
| 推荐迁入子集（去重，排除缓存/密钥/录音） | 2500–3500 | ~20–25 MB |
| 若误含大量重复 front/deploy | 更高 | 更高 |

## 第二阶段状态

- 盘点完成  
- **未执行任何复制**  
- 等待用户明确回复：**确认执行整理**
