# 待确认问题

生成时间：2026-08-12  
在你回复 **「确认执行整理」** 之前，请尽量确认下列项。未确认项将放入“待确认清单”，不会擅自删除源文件。

## 必须确认后才能大规模复制

1. **是否确认执行整理？**（原话触发词：`确认执行整理`）  
2. **核心范围是否同意：** 以  
   `E:\YinoVapi\.worktrees\yino-voice-stage1\YinoVoicePlatform`  
   为主源，映射到 `apps/control-plane/*` 与 `apps/runtime/*`？  
3. **LAN 增量是否纳入：**  
   - `packages/platform-core/`（来自 NAS `services/platform-core`）  
   - `deploy/`（来自 NAS `deploy-package`）  
   - `integrations/`（排除 `.env` 与本地 mapping）  
4. **原型是否进入 `archive/`：** STT-LLM-Hello World、fun-asr-workbench、minimal-stt-llm-tts、web-console、ai-voice-management（排除其 `.git`）？  
5. **`livekit-server.exe`（约 38.5MB）** 是否纳入 `bin/`，还是只写安装说明？  
6. **Stage2 Capacitor / android 增量** 是否合并进 web？还是本轮跳过？  
7. **根协作文件**（`README.md`、`AGENTS.md`、`SECURITY.md` 等）远程为空时可新建；若你另有标准模板请提供。

## `gh` CLI 状态（影响权限精确判断）

当前 Cursor 终端与 `winget list` **找不到已安装的 GitHub CLI**；`E:\gh CLI` 目录为空。

请你任选其一：

- 把 `gh.exe` 的完整路径发给我；或  
- 在 CMD 中执行并把输出发我（不要发 Token）：

```cmd
where gh
gh --version
gh api user --jq .login
gh repo view yzhan722/yinovoice --json visibility,viewerPermission,isEmpty,defaultBranchRef
```

## 推送策略（提交前）

远程为空仓库时，通常：

- 可在 `main` 首次推送；或  
- 若所有者启用了保护，则走新分支 + PR  

**本轮不会自动 commit/push。** 整理完成后由你用 GitHub Desktop 检查。

## 明确不需要你现在做的事

- 不要登录 `yzhan722` 账号  
- 不要在聊天中提供密码或 Token  
- 不要删除源项目里的 `.git`  
