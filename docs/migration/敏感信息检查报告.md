# 敏感信息检查报告

检查日期：2026-08-13

## 扫描原则

报告只记录类别和结论，不记录候选敏感值。扫描覆盖当前工作树的文件名、大小、常见密钥模式、配置、音频、备份、归档和生成物。

## 结果

- 未发现真实 `.env`；只存在 `.env.example`。
- 未发现私钥文件、明确的私钥块、GitHub/AWS 令牌格式或带明文认证信息的数据库 URL。
- 通用 secret 规则命中代码变量、测试数据、README、开发配置和示例文件；人工分类未确认真实凭据。
- 未发现真实录音或常见音频格式文件。
- 未发现数据库备份、压缩归档、客户导出目录或医疗数据目录。
- 未发现超过 50 MB 或 100 MB 的文件；工作树约 4.14 MB。
- 未发现 `node_modules`、虚拟环境、构建目录、缓存或日志目录。
- 发现三个已跟踪 `*.egg-info` 生成元数据目录；已补充忽略规则，未删除现有文件。

## 限制与要求

- 示例知识库涉及牙科业务，但当前内容表现为 Demo/合成资料；提交前仍应由业务负责人确认其不来源于真实客户或患者。
- 自动规则可能漏报编码、压缩、图片或自定义格式中的秘密；GitHub Desktop 提交前必须人工复核 diff。
- 如果任何候选值曾用于正式环境，应先轮换，不得仅依赖删除文件。
