---
description: yinovoice 项目级 Cursor 规则
alwaysApply: true
---

# yinovoice project rules

## 代码编辑

- 优先小范围改动；匹配现有命名与目录边界
- Control Plane（`apps/control-plane`）与 Runtime（`apps/runtime`）职责分离
- 不要把实时通话主链路逻辑放进 n8n 或仅依赖 Vapi Workflows

## 架构边界

- Yino = 业务数据与配置唯一事实来源
- Vapi = 适配器 / 兼容 / 备用渠道
- 录音最终应走向自有 S3 兼容存储

## 测试

- 改 API / agent / 前端核心路径时补或跑相关测试
- 不要在测试里写入真实密钥或客户数据

## 类型与错误处理

- Python 保持明确类型与可诊断错误信息（勿泄露密钥）
- 前端避免把密钥放进 `VITE_*`

## 数据库迁移

- 迁移必须可审阅、可回滚；禁止对生产库做无备份破坏性操作

## API 兼容性

- 对外 API 变更需考虑多租户与向后兼容；破坏性变更先文档化

## 客户数据脱敏

- 禁止提交真实客户/患者数据与通话录音
- 示例仅用合成数据

## Git 安全

- 未经用户明确要求：不 commit、不 push、不强推、不改 origin
- 禁止嵌套其他仓库的 `.git`

## 部署

- 不得自动部署生产
- 不得自动修改远程仓库权限或可见性
