# Deploy config

本目录用于部署环境变量**模板与说明**。

真实的 `platform-api.env` / `voice-agent.env` **不得**提交到 Git。  
迁移整理时若发现疑似正式配置，已移出仓库并隔离到本机报告目录的 quarantine 区。

请在服务器或私密配置渠道维护真实值，仓库内只保留 `*.env.example`（若有）或文档中的变量名清单。
