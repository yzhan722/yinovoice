# Local LiveKit --dev defaults only. Never reuse these values outside local development.

# REQUIRED (local-dev worker): LIVEKIT_URL, LIVEKIT_API_KEY, LIVEKIT_API_SECRET,
# DASHSCOPE_API_KEY, QWEN_REALTIME_URL.
# STAGE REQUIRED: also PLATFORM_API_URL, PHONE_LOOKUP_TOKEN, VOICE_RUNTIME_MODE=stage,
# and ALLOW_EMPTY_DISPATCH_METADATA_LOCAL_DEV=false.
# LOCAL DEV ONLY: ALLOW_EMPTY_DISPATCH_METADATA_LOCAL_DEV=true for `server console`.
# OPTIONAL: VOICE_OPS_*, VOICE_WORKER_DRAIN_TIMEOUT_SECONDS, VOICE_UX_*, LIVEKIT_AGENT_NAME.

LIVEKIT_URL=ws://localhost:7880
LIVEKIT_API_KEY=devkey
LIVEKIT_API_SECRET=secret
PLATFORM_API_URL=http://localhost:8000
ALLOW_EMPTY_DISPATCH_METADATA_LOCAL_DEV=false
# STAGE REQUIRED: must stay false. LOCAL DEV ONLY if you run `server console`.

# Runtime mode: local-dev | synthetic-test | stage
# STAGE REQUIRED: VOICE_RUNTIME_MODE=stage
# VOICE_RUNTIME_MODE=local-dev

# Optional worker drain timeout for Runtime-owned lifecycle cleanup (1-300).
# LiveKit AgentServer.drain_timeout stays 3600; do not confuse the two.
# VOICE_WORKER_DRAIN_TIMEOUT_SECONDS=30

# Optional loopback ops HTTP. Default disabled. Host defaults to 127.0.0.1.
# Never bind 0.0.0.0 without an infrastructure ACL.
# VOICE_OPS_ENABLED=false
# VOICE_OPS_HOST=127.0.0.1
# VOICE_OPS_PORT=8091
# Required for inbound SIP. Must match Control Plane PHONE_LOOKUP_TOKEN.
# Never put this value in VITE_* or commit it.
# PHONE_LOOKUP_TOKEN=replace-with-runtime-lookup-token

# Optional. Must match LiveKit Dispatch Rule roomConfig.agents[].agentName.
# LIVEKIT_AGENT_NAME=yino-customer-service

# Mid-call tools POST to Control Plane /api/v1/tool-invocations using PLATFORM_API_URL
# and the Demo X-Tenant-ID header. Leave unset tools as best-effort (never fail the call).



# Qwen Audio Realtime is the default customer-demo voice loop.
VOICE_PROVIDER_MODE=qwen-realtime
# Replace both placeholders below in .env.local with valid DashScope values.
DASHSCOPE_API_KEY=replace-with-your-dashscope-api-key
QWEN_REALTIME_URL=wss://your-workspace-id.cn-beijing.maas.aliyuncs.com/api-ws/v1/realtime
QWEN_REALTIME_MODEL=qwen-audio-3.0-realtime-plus
QWEN_REALTIME_VOICE=longanqian

# Optional Voice UX timers. Non-secret. Safe telephone defaults.
# VOICE_UX_INITIAL_SILENCE_S=8
# VOICE_UX_FOLLOWUP_SILENCE_S=12
# VOICE_UX_MAX_SILENCE_PROMPTS=2
# VOICE_UX_MAX_IDLE_S=180
# VOICE_UX_MAX_SESSION_S=1800
# VOICE_UX_TOOL_BRIDGE_AFTER_S=1
# VOICE_UX_MAX_ASSISTANT_TURN_S=45
# VOICE_UX_ENDPOINT_SILENCE_MS=450
# VOICE_UX_ENDPOINT_THRESHOLD=0.35


# Optional pipeline fallback (Fun-ASR + LLM + TTS): uncomment and configure.
# VOICE_PROVIDER_MODE=pipeline
# DASHSCOPE_WEBSOCKET_URL=wss://your-workspace-id.cn-beijing.maas.aliyuncs.com/api-ws/v1/inference
# FUN_ASR_MODEL=fun-asr-realtime
# OPENAI_API_KEY=replace-with-your-openai-api-key
# TTS_MODEL=gpt-4o-mini-tts
# TTS_VOICE=ash

# Pipeline LLM vendor. All of these speak the OpenAI chat protocol, so only
# the endpoint, model and key change. Unset behaves exactly as before
# (OpenAI + OPENAI_API_KEY + LLM_MODEL). TTS still uses OPENAI_API_KEY.
#   openai      gpt-4o-mini            OPENAI_API_KEY
#   deepseek    deepseek-chat          DEEPSEEK_API_KEY
#   dashscope   qwen-plus              DASHSCOPE_API_KEY   (same key as Qwen Realtime)
#   zhipu       glm-4-plus             ZHIPU_API_KEY
#   moonshot    moonshot-v1-8k         MOONSHOT_API_KEY
#   minimax     abab6.5s-chat          MINIMAX_API_KEY
#   siliconflow Qwen/Qwen2.5-7B-Instruct  SILICONFLOW_API_KEY
#   openrouter  openai/gpt-4o-mini     OPENROUTER_API_KEY
#   custom      (requires LLM_BASE_URL + LLM_MODEL)  LLM_API_KEY
# LLM_PROVIDER=deepseek
# LLM_MODEL=deepseek-chat          # optional: overrides the provider default
# LLM_BASE_URL=                    # optional: gateway or self-hosted endpoint
# LLM_API_KEY=                     # optional: overrides the provider key env
# DEEPSEEK_API_KEY=replace-with-your-deepseek-api-key
