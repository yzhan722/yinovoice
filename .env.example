# Root placeholder only. Copy app-level examples instead of putting secrets here.
# Never commit real .env / .env.local files.

# Control Plane API
#   apps/control-plane/api/.env.example  ->  .env.local

# Runtime voice agent
#   apps/runtime/voice-agent/.env.example  ->  .env.local

# Web console
#   apps/control-plane/web/.env.example  ->  .env.local

# Optional local Postgres (see docker-compose.yml)
# DATABASE_URL=postgresql+asyncpg://yino:yino@127.0.0.1:5432/yino_platform
