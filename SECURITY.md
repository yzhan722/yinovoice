# SECURITY.md

## 密钥不得进入 Git

- 禁止提交 `.env`、`.env.local`、`.env.production` 及同类文件  
- 禁止提交 `*.pem` / `*.key` / `credentials/` / `secrets/`  
- `deploy/config/*.env` 真实配置禁止入库（仅允许 example/说明）  

## 客户信息必须脱敏

- 禁止上传真实手机号、邮箱、住址、身份证、合同发票、财务资料  
- 文档与提示词示例必须使用合成/脱敏数据  

## 通话录音不得进入仓库

- 禁止提交 `recordings/`、`*.webm` / `*.wav` / `*.mp3` 等通话音频  
- Demo 本地录音目录仅供本机调试  

## 录音应转存到自有 S3 兼容存储

- 生产录音与转写元数据应落在 Yino 可控对象存储  
- 外部语音平台不得作为唯一录音存储  

## `.env.example` 规则

- 只保存变量名与安全占位值（如 `replace-with-...`、本地 `devkey` 说明）  
- LiveKit `--dev` 约定值仅限本机开发，不得用于共享/生产  

## 发现凭证泄露后的处理

1. 立刻轮换泄露凭证（API Key、DB、S3、OAuth 等）  
2. 从 Git 历史中移除敏感文件（需要时由仓库维护者处理；禁止强推除非所有者明确要求）  
3. 检查访问日志与滥用迹象  
4. 在 `DECISIONS.md` 或安全记录中留下事件摘要（不含密钥本身）  

## 凭证轮换要求

- 人员变动、疑似泄露、供应商轮换周期到达时必须轮换  
- 生产与本地开发凭据分离  

## 日志脱敏要求

- 禁止打印 Authorization、Cookie、完整 Token、录音 URL 签名串  
- 客户标识优先使用内部 UUID  

## 安全问题报告方式

- 优先私信仓库维护者 / 所有者 `yzhan722` 与当前协作者  
- 不要在公开 Issue 中粘贴密钥或客户数据  
